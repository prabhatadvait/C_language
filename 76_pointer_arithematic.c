#include<stdio.h>

int main(){
    // int i= 4;
    // int *ptr;
    // ptr = &i;
    // printf("The value of ptr is %u\n",ptr);
    // // ptr-- = ptr -1;
    // // ptr++ = ptr + 1;
    // ptr++;
    // printf("The value of ptr is %u\n",ptr);   
    
    // char c ='p';
    // char *ptr = &c;
    // printf("The value of ptr is %u\n",ptr);
    // ptr++;
    // printf("The value of ptr is %u\n",ptr);
    // 

    float k = 3.8;
    float *ptr;
    ptr = &k;
    printf("The value of ptr is %u\n",ptr);
    ptr++;
    printf("The value of ptr is %u\n",ptr);
    return 0;
}
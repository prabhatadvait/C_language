#include<stdio.h>

int main(){
    int radius = 3;
    float pi = 3.14;
    printf("The area of a circle is %f \n", pi*radius*radius);
    int height = 3;
    printf("The volume of a cylinder is %f \n",pi*radius*radius*height);
}